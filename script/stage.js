/**
 * BreakOut | stage.js
 * (c) 2021 mono / Gabuniku
 */
// @ts-check
//ステージデータ
import {Pos,Rect} from "./util.js"

/**
 * ステージは当たり判定の計算量削減のためステージ全体を4分割し、該当するエリアにあるブロックとだけ衝突処理をする。
 */

/**
 * エリアを返す
 * @param {Pos} pos 
 */
export function getArea(pos){}


export const TestStage = []